# Copy this file to .env for local development.
# The foundation currently uses only the application settings below.
# NEVER commit the .env file — it is in .gitignore.

APP_PORT=8000
APP_ENV=development
FLEET360_DATA_DIR=src/data
